package opendof.sql;

/**
 * A dummy abstract class that implements SQLValidatorInterface. This is provided in order to have consistency between SQLValidator and SQLProvider objects.
 * @see SQLValidatorInterface
 */
public abstract class SQLValidator implements SQLValidatorInterface{
}
